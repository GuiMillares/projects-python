# Copie para .env.local se precisar mudar algo. O padrão já funciona.
#
# Endereço da API local (Flask + MySQL do Laragon).
VITE_API_BASE=http://127.0.0.1:8756

# O token da brapi NÃO vem para cá: ele é do backend (FIN_BRAPI_TOKEN).
# Colocar um segredo num VITE_* o publica dentro do bundle do frontend.
