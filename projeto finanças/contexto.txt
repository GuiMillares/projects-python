CONTEXTO DO PROJETO: Painel financeiro pessoal
==============================================
Cole este arquivo no Claude Code de casa (ou do trabalho) para retomar o
trabalho.
Última atualização: 18/08/2026 (refino das Metas: escada de 300 níveis, modais, abas)


1. O QUE É
----------
App de finanças pessoais de dono único. Front-end React (Vite) que no futuro
roda como desktop nativo via Tauri, falando com uma API Python (Flask) local
sobre o MySQL do Laragon.

Repositório: git@github.com:GuiMillares/projects-python.git (branch main)
O projeto fica na subpasta "projeto finanças/" do repositório.


2. ESTRUTURA
------------
projeto finanças/
  db/schema.sql        cria o banco local_finance do zero (idempotente)
  db/migrar.sql        alterações em banco que já existe (idempotente)
  api/                 API Flask
    app.py             rotas
    db.py              conexão MySQL (PyMySQL), sempre com parâmetro %s
    seguranca.py       senha (scrypt), sessões e TOTP
    cotacoes.py        cliente brapi.dev (cotação da B3)
    gamificacao.py     regras de XP, nível e bônus (funções puras)
    ia.py              ÚNICO arquivo que chama um LLM (Google Gemini)
    config.py          tudo configurável por variável de ambiente
    criar_usuario.py   CLI para criar/trocar senha do usuário
    importar_sqlite.py traz o financas.db antigo para o MySQL
  ui/                  React + Vite
    src/pages/         Login, Dashboard, Lancamentos, Investimentos,
                       Configuracoes, EmBreve
    src/components/    AppLayout, Charts (SVG próprios)
    src/services/      api, authService, financasService,
                       investimentosService, metasService, perfilService,
                       cores, imagem
    src/styles/        tokens.css (TODA a cor), app.css (layout)
  financas.py          CLI antiga em SQLite, origem dos dados a importar
  README.md            instruções detalhadas


3. COMO SUBIR
-------------
a) MySQL do Laragon (pelo painel do Laragon, ou):
   C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysqld.exe \
     --defaults-file=C:/laragon/bin/mysql/mysql-8.4.3-winx64/my.ini

b) Banco, RODANDO DA RAIZ "projeto finanças/":
   mysql -u root --skip-password < db/schema.sql
   mysql -u root --skip-password < db/migrar.sql

c) API (ATENÇÃO: ative o venv certo, ver seção 7):
   cd api
   source ../../.venv/Scripts/activate
   pip install -r requirements.txt      # só na primeira vez
   python criar_usuario.py              # só na primeira vez
   python app.py                        # http://127.0.0.1:8756

d) Telas:
   cd ui
   npm install
   npm run dev                          # http://localhost:1420

No primeiro acesso a tela pede o cadastro do 2FA e mostra o QR code.


4. DECISÕES DE ARQUITETURA (não desfazer sem motivo)
-----------------------------------------------------
* NÃO usa Firebase. O projeto começou com Firebase Auth + Cloud Functions
  (copiado do painel /superadmin do Synchro) e foi migrado para processo
  local a pedido. O mapeamento foi:
      Firebase Auth        -> senha com hashlib.scrypt em seguranca.py
      Cloud Functions 2FA  -> rotas /api/auth/2fa/*
      Documento Firestore  -> tabela usuarios

* INVARIANTE DE SEGURANÇA: o segredo TOTP nunca existe no frontend. É
  gerado no servidor, mora só em usuarios.totp_secret e nenhuma rota o
  devolve. O cliente vê uma única vez o otpauth:// do cadastro (para virar
  QR code) e depois só manda 6 dígitos e recebe sim ou não.
  Se aparecer otplib ou speakeasy no ui/package.json, a regra foi quebrada.

* Login devolve sessão PARCIAL: ela só abre as rotas de 2FA. Nenhum dado
  financeiro sai antes do código ser validado.

* Código TOTP vale UMA vez (janela queimada em usuarios.totp_ultimo_step),
  não os 30 segundos inteiros.

* Dinheiro é DECIMAL no MySQL, nunca FLOAT.

* Toda query passa por parâmetro (%s), nunca por f-string.

* A API escuta só em 127.0.0.1 e aceita uma lista fechada de origens CORS.

* app.py checa a porta antes de subir e recusa se já houver algo lá. Isso
  existe porque o Windows deixa DOIS processos escutarem a mesma porta
  (SO_REUSEADDR), e as respostas passam a se revezar entre eles; quando um
  é de antes de uma alteração, a API parece rodar código velho de forma
  intermitente. Isso já causou confusão duas vezes.


5. DESIGN (o dono é exigente com isso, ler antes de mexer)
-----------------------------------------------------------
Ele rejeita estética de "template gerado por IA". Regras concretas:

* Paleta "Oliva & Terracota". Base neutra QUENTE (marrom-tinta no escuro,
  pergaminho no claro) com acento OLIVA (#A8BD52 escuro, #556B18 claro).
  PROIBIDO: ciano, azul e teal (é a cara do Synchro, projeto dele que ele
  quer diferenciar) e roxo/indigo de template.
* Entradas em oliva, saídas em terracota, alerta em ocre, ação urgente em
  tijolo. Os quatro em matizes bem separados.
* TODA cor vive em ui/src/styles/tokens.css. Nenhum componente tem cor
  fixa. É o que faz o tema claro/escuro funcionar sem duplicar componente.
  A única exceção documentada eram as amostras do seletor de tema, que já
  foi removido.
* Sem gradiente ou glassmorphism decorativo. Sem excesso de card com sombra
  suave e canto muito arredondado.
* Ícones lucide-react. NENHUM emoji na interface.
* SEM TRAVESSÃO (o traço longo) em texto nenhum, nem em comentário. Pediu
  explicitamente para tirar todos; use dois-pontos, vírgula ou ponto.
* Gráficos são componentes SVG próprios em components/Charts.jsx. Não
  adicionar biblioteca de gráfico.
* Botão primário no hover INVERTE: fundo transparente, borda e texto na cor
  do acento.
* Responsivo com breakpoint em 900px: sidebar no desktop, bottom navigation
  no mobile. Verificado sem rolagem lateral até 320px.
* Cor de categoria vem da POSIÇÃO na lista ordenada (services/cores.js),
  não de hash do nome. Hash colidia e três categorias saíam iguais.


6. ESTADO DAS TELAS
-------------------
PRONTAS:
  Login + 2FA (cadastro com QR e verificação)
  Visão geral (KPIs, gráficos, pontos de atenção)
  Entradas e Saídas
  Investimentos (carteira com cotação da B3)
  Metas gamificadas (XP, nível, subtarefas sugeridas por IA)
  Configurações (perfil, foto, senha, 2FA, orçamentos)

NÃO HÁ MAIS TELA PLACEHOLDER. O componente EmBreve.jsx foi removido.

Entradas e Saídas são o MESMO componente (pages/Lancamentos.jsx) com a prop
`natureza` trocada ("receita" / "despesa"). Não duplicar.


7. ARMADILHAS DO AMBIENTE (isso já custou tempo)
-------------------------------------------------
* Existem DOIS venvs em ~/Pictures/guizao/python: `.venv` (com Flask,
  PyMySQL e pyotp) e `venv` (vazio, sobra antiga). O ~/.bashrc, nas linhas
  91-96, ativa automaticamente o `venv` ERRADO a cada shell novo. Sintoma:
  "ModuleNotFoundError: No module named 'flask'".
  Solução: source ../../.venv/Scripts/activate
  Ou chamar direto: ../../.venv/Scripts/python.exe app.py
  (O bloco no .bashrc também tenta rodar citacoes.py e falha em toda
   abertura de terminal. Ele foi deixado como está, é arquivo pessoal.)

* Nunca subir duas instâncias de app.py. Ver seção 4.

* O scrypt precisa de maxmem declarado: o OpenSSL corta em 32MB e n=2^15
  encosta exatamente nesse teto. Já está resolvido em seguranca.py.

* PC NOVO (ex: do trabalho): o Laragon não vem instalado sozinho. Sem ele
  o MySQL não existe e a API sobe normal mas todo request ao banco cai com
  "ConnectionRefusedError" / pymysql OperationalError 2003, parecendo bug
  de código quando na verdade é só instalar o Laragon (laragon.org) antes
  do passo 3a e rodar o passo 3b (schema.sql + migrar.sql) pela primeira
  vez nesse PC.

* Variável de ambiente PERMANENTE do Windows (setx / SetEnvironmentVariable
  com escopo "User", ver seção 8 sobre FIN_BRAPI_TOKEN) só é enxergada por
  processos abertos DEPOIS de configurada. Um terminal, VS Code ou Claude
  Code que já estava aberto continua sem ver o valor até ser fechado e
  reaberto de verdade. Isso já confundiu um teste (a API parecia sem
  token mesmo depois de configurado).


8. COTAÇÃO DA B3 (brapi.dev)
-----------------------------
Investimentos guarda ticker, quantidade e preço médio. O valor atual NÃO é
gravado: é quantidade x cotação do momento.

A brapi descontinuou a v1 (/api/quote/{tickers}?token=) e migrou para v2:
  GET {BRAPI_BASE}/v2/stocks/quote?symbols=TICKER1,TICKER2
com o token (quando existe) no header "Authorization: Bearer SEU_TOKEN",
não mais em ?token= (isso agora só vale pra ferramentas no-code, segundo
a doc deles). O payload de cada item também mudou: os números ficam
aninhados em "data" (res["data"]["regularMarketPrice"]), antes era plano
no próprio item. cotacoes.py já foi corrigido pra isso em 17/08/2026; se
voltar a dar 401 mesmo com token certo, a API pode ter mudado nome do
campo de novo, vale conferir a doc em brapi.dev/docs.

TOKEN: sem token a brapi só responde SEM autenticação pra um punhado de
tickers conhecidos (PETR4, VALE3, MGLU3, ITUB4...) e recusa o resto com
401 MISSING_TOKEN. O código cobre os dois casos:
  sem token -> um request por ticker, no máximo 6 (FIN_BRAPI_MAX_SEM_TOKEN)
  com token -> um request só com a carteira inteira

>>> TOKEN JÁ GERADO em brapi.dev (17/08/2026). NÃO está neste arquivo nem
>>> em nenhum arquivo do repositório de propósito (segredo não vai pro
>>> git). Guardado neste PC como variável de ambiente PERMANENTE do
>>> Windows. Pegue o valor num cofre de senhas, não aqui.
>>>
>>> Em QUALQUER outro computador (ex: do trabalho) isso precisa ser
>>> configurado de novo, uma vez por PC:
>>>   permanente (uma vez, PowerShell):
>>>     [Environment]::SetEnvironmentVariable("FIN_BRAPI_TOKEN", "SEU_TOKEN", "User")
>>>   só na sessão atual, pra testar rápido:
>>>     $env:FIN_BRAPI_TOKEN = "seu_token"     # PowerShell
>>>     export FIN_BRAPI_TOKEN=seu_token       # Git Bash
>>> Ver a pegadinha da variável permanente na seção 7 (só terminal aberto
>>> DEPOIS de configurar enxerga o valor).

Cache de 60s para não estourar o limite gratuito a cada F5.

FALLBACK: cotacoes.buscar NUNCA levanta exceção. Devolve (cotações, aviso).
Se a brapi cair, as posições aparecem completas sem os números de mercado e
um aviso discreto explica. Testado com host inexistente: HTTP 200, itens
completos, cotacao null.

FORMULÁRIO DE NOVO INVESTIMENTO: ao digitar um ticker que já parece
completo (4 letras + 1 ou 2 dígitos, ex: PETR4, HGLG11), a tela busca a
cotação sozinha (debounce de 500ms, rota GET /api/cotacoes/<ticker>) e
preenche "Preço médio" com o valor atual, só pra facilitar. Editar esse
campo na mão tem prioridade e para o preenchimento automático: é o preço
que a pessoa realmente pagou que importa, não o preço de agora.


9. O QUE JÁ FOI TESTADO
------------------------
Backend, ponta a ponta contra a API rodando:
  44 asserções no fluxo de auth/2FA/transações/orçamentos
  23 asserções em transações + investimentos + isolamento entre usuários
  5 asserções no fallback da cotação (API apontada para host inexistente)

Frontend, com Playwright/Chromium em dark+light e desktop+mobile:
  17 asserções nas três telas novas, incluindo adicionar e excluir de
  verdade, cotação viva do PETR4 e ticker inválido marcado como sem cotação
  12 asserções na foto de perfil
  33 asserções em metas/subtarefas: XP subindo e voltando, bônus maior que
  a soma, isolamento entre usuários, cascade ao apagar a meta
  21 asserções em api/ia.py com cliente simulado (parsing, corte no teto,
  XP preso à faixa, cada erro do SDK virando texto útil)
  23 asserções na tela de Metas no browser
  Zero erros de runtime, sem rolagem lateral a 320px

NÃO TESTADO: a chamada real ao Gemini. Não havia GEMINI_API_KEY no ambiente
onde o código foi escrito, então o que foi verificado é tudo em volta dela
(o que é enviado, o que é feito com a resposta, cada tipo de falha), mas a
ida à rede em si nunca rodou. É o único trecho sem verificação de execução.


9b. METAS E IA
---------------
XP e nível NÃO são gravados: derivam do que está concluído
(api/gamificacao.py). Bônus da meta = 1.5x a soma das subtarefas, mínimo
50, sempre maior que a soma. Curva: nível 2 aos 100 XP, 3 aos 300, 4 aos
600.

A quebra da meta em passos vive em api/ia.py, o ÚNICO arquivo que fala com
um LLM. Provedor: GOOGLE GEMINI (SDK google-genai), escolhido em 18/08/2026
por causa do tier gratuito sem cartão. Saída estruturada por JSON schema,
thinking em nível LOW, modelo padrão gemini-3.5-flash (trocável por
FIN_IA_MODELO: gemini-3.5-flash-lite é mais barato, gemini-3.7-flash é
melhor).

O contrato do módulo é (subtarefas, aviso) e ele NUNCA levanta exceção. Foi
o que deixou a troca de provedor (Anthropic -> Gemini) custar um arquivo
só: rotas, banco, gamificação e tela não souberam da mudança.

REFINOS DE 18/08 (tarde), todos testados inclusive com chamada REAL ao
Gemini (a chave foi configurada neste PC):
* Geração mais inteligente: prompt exige passos específicos da meta (com
  contas reais de aporte/prazo), thinking MEDIUM (FIN_IA_THINKING), e a
  rota de "sugerir mais" manda os passos existentes para o modelo
  complementar; ia.py ainda filtra repetição literal (case/acento/
  pontuação) via _normalizar. Teste real: meta de viagem ao Japão, 2a
  leva veio 100% inédita (seguro, eSIM, Suica, câmbio, Visit Japan Web).
* Escada de níveis: 300 aparências únicas em ui/src/services/niveis.js
  (15 patamares x 4 divisões IV-I x 5 pips, pesquisado em LoL/Duolingo/
  insígnias militares). gamificacao.py trava o nível em NIVEL_MAXIMO=300.
* Exclusões confirmam num modal (components/ConfirmarExclusao.jsx), usado
  em Metas (meta e passo), Entradas/Saídas e Investimentos. Foco inicial
  no Cancelar; Esc e clique no véu fecham.
* Metas concluídas vão para a aba "Concluídas" (filtro fin-seg com
  contadores); concluir uma meta a move na hora.

>>> PENDENTE: GEMINI_API_KEY ainda não foi configurada. Sem ela a tela
>>> funciona, só que a caixa "sugerir os passos" vem desabilitada
>>> explicando o que falta. Gere em aistudio.google.com/apikey e configure:
>>>   [Environment]::SetEnvironmentVariable("GEMINI_API_KEY","AIza...","User")
>>> A chave NÃO fica em arquivo nenhum do repositório: o SDK a lê do
>>> ambiente sozinho. O SDK também aceita GOOGLE_API_KEY (com precedência),
>>> mas prefira GEMINI_API_KEY para não colidir com outra ferramenta.


10. PRÓXIMOS PASSOS SUGERIDOS
------------------------------
1. Configurar GEMINI_API_KEY para ligar a sugestão de passos por IA.
2. FASE 2 combinada e NÃO iniciada: duplicar o projeto para
   "projeto financas-firestore/" trocando Flask+MySQL por Firebase Auth +
   Firestore, com espaços (workspaces) compartilhados para casal e
   empresas. O projeto atual continua intacto rodando no VPS por mais duas
   semanas. ANTES de codar essa fase é OBRIGATÓRIO pedir ao dono: as
   credenciais do SDK web, se é projeto Firebase novo ou o mesmo do
   Synchro (recomendar separar), e se Auth/Firestore já estão habilitados.
3. Empacotar com Tauri: tauri.conf.json aponta build.devUrl para
   http://localhost:1420 e a API sobe como sidecar.
   ui/src/services/api.js é o ÚNICO arquivo que sabe o endereço do
   servidor; se a comunicação virar invoke() do Tauri, é só ali que muda.
3. Editar lançamento existente (hoje só cria e exclui).

Feito em 17/08/2026: token da brapi gerado e configurado (ver seção 8),
API corrigida pra v2 da brapi, e o formulário de investimento agora sugere
o preço médio a partir da cotação ao vivo do ticker digitado.
